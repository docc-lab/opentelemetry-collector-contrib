type: overlapdetection

status:
  class: processor
  stability:
    development: [traces]
  distributions: [contrib]
  codeowners:
    active: [tomislavzm]

tests:
  config:
    overlap_threshold_ms: 1.0
    enable_metrics: true
